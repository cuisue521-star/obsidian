- **HTML（骨架）**
    
    `<div>、<h2>、<p>、<button>` 决定页面有盒子、标题、文字、按钮这些元素。
    
- **CSS（样式）**
    
    设置背景色、盒子圆角、阴影、按钮颜色、鼠标悬浮变色、居中排版，控制好不好看。
    
- **JavaScript（交互）**
    
    监听按钮点击，点击后自动修改段落文字、文字颜色，实现动态交互。